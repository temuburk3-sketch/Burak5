# Project Rules & Preferences

- **Firestore Security Rules Reminder**: Every time a new feature is added or changes are made to the app, include a short note at the end reminding the user about activating/hardening Firestore Security Rules (`firestore.rules`) once the project is finished and ready for production.
